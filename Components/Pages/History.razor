@page "/History"
@using Microsoft.AspNetCore.Components.Routing
@using AnimeMaze.Data
@using AnimeMaze.Models

<PageTitle>Historia - Anime Maze</PageTitle>
<HistoryHeader/>
<main class="min-h-screen bg-gradient-to-b from-gray-900 via-gray-800 to-gray-900 text-gray-100 pb-24">
    <!-- Botón fijo -->
    <div class="fixed bottom-6 right-6 z-50 animate-bounce-slow">
        <NavLink href="/MazeSizeSelection" 
                class="bg-gradient-to-r from-pink-600 to-purple-700 text-white font-bold py-4 px-8 rounded-full 
                       shadow-xl transform transition-transform hover:scale-110 flex items-center gap-2">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/>
            </svg>
            ¡COMENZAR AVENTURA!
        </NavLink>
    </div>

    <article class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-12 space-y-16">
        <!-- Sección Principal -->
        <header class="text-center space-y-8 animate-fade-in">
            <div class="relative inline-block">
                <img src="/images/Map Elements/Exit.png" 
                     alt="Flor Eterna de Sakura" 
                     class="w-48 h-48 mx-auto pixel-art transform rotate-12 hover:rotate-0 transition-transform"/>
                <div class="absolute inset-0 bg-gradient-to-r from-pink-500/20 to-purple-500/20 rounded-full blur-xl"></div>
            </div>
            <h1 class="text-4xl md:text-5xl font-bold bg-gradient-to-r from-pink-400 to-purple-400 bg-clip-text text-transparent">
                La Batalla por la Inmortalidad
            </h1>
            <div class="max-w-3xl mx-auto space-y-6 text-gray-300 text-lg leading-relaxed">
                <p>
                    En un mundo donde la inmortalidad es el mayor tesoro, la legendaria <strong class="text-pink-300">Flor Eterna de Sakura</strong> atrae a los guerreros más poderosos de todos los rincones del multiverso anime. Esta flor mítica, escondida en el corazón de un laberinto traicionero, promete vida eterna... pero solo para aquel que pueda superar sus desafíos.
                </p>
            </div>
        </header>

        <!-- Sección Isla Shinsenkyo -->
        <section aria-labelledby="isla-heading" class="bg-gray-800/40 p-8 rounded-xl">
            <h2 id="isla-heading" class="text-3xl font-bold text-emerald-400 text-center mb-8">Isla de Shinsenkyo</h2>
            <div class="grid md:grid-cols-2 gap-8 items-center">
                <div class="space-y-4">
                    <p class="text-gray-300 leading-relaxed">
                        Creada por un antiguo dios para probar a los mortales, esta isla está cubierta de cerezos eternos que esconden peligros mortales. En su corazón late el laberinto encantado, cuyas reglas cambian con cada luna llena.
                    </p>
                </div>
                <img src="/images/Obstacles/Rock.png" 
                     alt="Vista de la isla" 
                     class="w-64 h-64 mx-auto pixel-art animate-float"/>
            </div>
        </section>

        <!-- Sección Trampas -->
        <section aria-labelledby="trampas-heading" class="space-y-12">
            <h2 id="trampas-heading" class="text-3xl font-bold text-red-400 text-center mb-8">Trampas Letales</h2>
            
            <div class="grid md:grid-cols-3 gap-8">
                @foreach (var trap in TrapData.Traps)
                {
                    <div class="bg-gray-800/40 p-6 rounded-xl border border-red-900/50 hover:border-red-400/30 transition-all">
                        <div class="flex items-center gap-4 mb-4">
                            <img src="@trap.Image" 
                                 alt="@trap.Name" 
                                 class="w-16 h-16 pixel-art"/>
                            <h3 class="text-xl font-semibold text-red-300">@trap.Name</h3>
                        </div>
                        <p class="text-gray-400 text-sm">@trap.Description</p>
                        <div class="mt-4 flex gap-2">
                            <span class="px-2 py-1 bg-red-900/50 text-red-300 text-xs rounded-full">@GetTrapType(trap)</span>
                        </div>
                    </div>
                }
            </div>
        </section>

        <!-- Sección Obstáculos -->
        <section aria-labelledby="obstaculos-heading" class="space-y-12">
            <h2 id="obstaculos-heading" class="text-3xl font-bold text-blue-400 text-center mb-8">Obstáculos del Laberinto</h2>
            
            <div class="grid md:grid-cols-2 gap-8">
                <div class="bg-gray-800/40 p-6 rounded-xl">
                    <div class="flex items-center gap-4 mb-4">
                        <img src="/images/Obstacles/Rock.png" 
                             alt="Roca" 
                             class="w-16 h-16 pixel-art"/>
                        <h3 class="text-xl font-semibold text-blue-300">Barreras Naturales</h3>
                    </div>
                    <p class="text-gray-400 text-sm">
                        El terreno mismo conspira contra ti con rocas gigantes y campos de lava que deberás superar usando tu ingenio y habilidades.
                    </p>
                    <div class="mt-4">
                        <div class="flex gap-2 mb-2">
                            <span class="px-2 py-1 bg-blue-900/50 text-blue-300 text-xs rounded-full">Vida: 100 HP</span>
                        </div>
                        <progress value="100" max="100" class="w-full h-2 bg-gray-700 rounded"></progress>
                    </div>
                </div>
            </div>
        </section>

        <!-- Sección Estrategia -->
        <section class="bg-gray-800/40 p-8 rounded-xl text-center space-y-6">
            <h2 class="text-2xl font-bold text-emerald-400">Claves para Sobrevivir</h2>
            <div class="grid md:grid-cols-3 gap-6">
                <div class="p-4 bg-gray-700/30 rounded-lg">
                    <div class="text-pink-400 text-2xl mb-2">⏱️</div>
                    <h3 class="text-lg font-semibold text-gray-200">Gestión de Turnos</h3>
                    <p class="text-sm text-gray-400 mt-2">Cada movimiento cuenta, planifica cuidadosamente tus acciones</p>
                </div>
                <div class="p-4 bg-gray-700/30 rounded-lg">
                    <div class="text-blue-400 text-2xl mb-2">⚡</div>
                    <h3 class="text-lg font-semibold text-gray-200">Efectos Temporales</h3>
                    <p class="text-sm text-gray-400 mt-2">Algunas trampas afectan tus stats por múltiples turnos</p>
                </div>
                <div class="p-4 bg-gray-700/30 rounded-lg">
                    <div class="text-purple-400 text-2xl mb-2">🛡️</div>
                    <h3 class="text-lg font-semibold text-gray-200">Prevención</h3>
                    <p class="text-sm text-gray-400 mt-2">Observa patrones en las trampas y aprende a evitarlas</p>
                </div>
            </div>
        </section>

        <!-- Sección Final -->
        <section class="bg-gradient-to-r from-pink-900/40 to-purple-900/40 p-8 rounded-xl text-center">
            <h2 class="text-2xl font-bold text-yellow-400 mb-4">El Verdadero Desafío</h2>
            <p class="text-gray-300 max-w-2xl mx-auto">
                La Flor Eterna no solo prueba tu fuerza, sino tu voluntad. ¿Qué estás dispuesto a sacrificar por la inmortalidad? Cada trampa superada y obstáculo vencido te acerca a la respuesta... y a tu destino final.
            </p>
        </section>
    </article>
</main>

<style>
    .pixel-art {
        image-rendering: pixelated;
        filter: drop-shadow(0 0 8px rgba(192, 132, 252, 0.1));
    }

    @@keyframes float {
        0%, 100% { transform: translateY(0); }
        50% { transform: translateY(-20px); }
    }

    .animate-float {
        animation: float 4s ease-in-out infinite;
    }

    @@keyframes bounce-slow {
        0%, 100% { transform: translateY(0); }
        50% { transform: translateY(-10px); }
    }

    .animate-bounce-slow {
        animation: bounce-slow 3s infinite;
    }

    progress::-webkit-progress-value {
        background: linear-gradient(to right, #3B82F6, #8B5CF6);
        border-radius: 4px;
    }
</style>

@code {
    private string GetTrapType(Trap trap)
    {
        return trap.Name switch
        {
            "Trampa de Veneno" => "Veneno",
            "Trampa de Parálisis" => "Electrocutar",
            "Trampa de Rc Cells" => "Sangrado",
            _ => "General"
        };
    }
}